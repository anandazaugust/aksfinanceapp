apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-frontend-to-backend
  namespace: backend   # 👈 change to your namespace
spec:
  podSelector:
    matchLabels:
      app: financeappbackend      # 👈 target pods (backend pods)
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: financeapp   # 👈 only allow pods with this label
    ports:
    - protocol: TCP
      port: 3000                    # 👈 backend app port
  egress:
  - {}  # allow all outbound (optional, can restrict later)
